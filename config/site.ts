export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "BC's Certsy",
	description: "",
	navItems: [
		{
			label: "Home",
			href: "/",
		}
	],
	navMenuItems: [

	],
	links: {

	},
};
